spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  kafka:
    topics:
      cv-parsed: cv.parsed
      status-changed: candidate.status.changed
